function id(monArgument) {
  // Le mot-clé "return" dit à la fonction de renvoyer la valeur
  return monArgument; 
}

function getLength(element) {
  // En JavaScript, on colle ".length" à la fin de l'élément pour avoir sa taille
  return element.length; 
}